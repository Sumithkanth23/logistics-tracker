{
  "rules": {
    "vehicles": {
      "$vehicleId": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    },
    "allowedUsers": {
      ".read": "auth != null",
      ".write": "auth != null && root.child('admins').child(auth.uid).exists()"
    }
  }
}
